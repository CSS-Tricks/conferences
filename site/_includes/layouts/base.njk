<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>{{ title }}</title>
    <link
      href="https://fonts.googleapis.com/css?family=Rubik:700"
      rel="stylesheet"
    />
    <link rel="stylesheet" href="{{ '/css/index.css' | url }}" />
    <link rel="stylesheet" href="{{ '/css/grid.css' | url }}" />
    <link rel="stylesheet" href="{{ '/css/list.css' | url }}" />
  </head>
  <body class="grid">
    {{ content | safe }}

    {% include "icons.njk" %}

    <script src="{{ '/script/index.js' | url }}"></script>
  </body>
</html>
